# 60-api — client APIs

## Hand-authored

- `python-api.md`  ★ session / run / upload / async / streaming.
- `java-api.md`    — DBConnection, BasicTable, type map.
- `cpp-api.md`     — DBConnection, ConstantSP, type map.
- `type-mapping.md` ★ cross-language type table (DolphinDB ↔ Python / Java / C++ / JDBC).

## Full upstream reference pages

Under ``:

- `connapi_intro.md` — overview of all APIs.
- `cpp/` — C++ API pages.
- `python/` — Python API pages.
- `tools/` — third-party tool integrations (DBeaver, Grafana, VSCode, Jupyter, Superset, Power BI, SQLTools, DataX).

Full manuals for **Java / JavaScript / R / Rust / Go / C# / REST / JDBC /
ODBC / JSON / AI DataLoader** are hosted externally under
<https://docs.dolphindb.cn/>; this skill summarizes the essentials but does
not mirror the full external docs.

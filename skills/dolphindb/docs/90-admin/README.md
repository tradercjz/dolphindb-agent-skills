# 90-admin — cluster operations

## Hand-authored

- `cluster.md`           — nodes, config, start/stop, rolling upgrade.
- `backup-restore.md`    — `backup`, `restore`, `backupDB`, `backupTable`.
- `security.md`          — users, groups, ACL, function views.

## Other files in this directory

From `sys_man/` (cluster mgmt, perf monitoring, backup, security, login).

From `db_distr_comp/cfg/` → `cfg/` (configuration parameters).

From `omc/` → `omc/` (operational troubleshooting guides — connection lost,
server hang, slow I/O).

See also `reference/error-codes/` for `S04xxx` (admin) and `S05xxx`
(general) errors.
